subprojects {
    buildscript {
        repositories {
            google()
            jcenter()
        }
    }
    repositories {
        google()
        jcenter()
    }
}

apply from: rootProject.file('dependencies.gradle')
